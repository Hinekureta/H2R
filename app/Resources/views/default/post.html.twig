{% extends 'base.html.twig' %}
{% block body %}
    <p>
        <a href="{{ url('delete-post', {'id' : post.id}) }}">Delete</a>
        {{ post.date | date('d-m-Y') }}
        <h2>{{ post.title }}</h2>
        {{ post.content |raw }}
    </p>
{% endblock %}